import { View, ActivityIndicator, Dimensions, Platform } from "react-native";

 
 const Loader = () => {
   return (
     <View>
       <Text>Loader</Text>
     </View>
   )
 }
 
 export default Loader